# 온에어킷 (onairkit) — 프로젝트 문서

> 이 문서는 `_ref/원안_계획서.md` 를 위젯 5종 완성 시점 기준으로 갱신한 최신판이다.
> 원안의 전략·원칙은 그대로 유지하고, 결정 대기 항목을 확정 사항으로 옮기고, 현재 상태와 다음 작업을 다시 썼다.
> 앞으로 이 문서가 기준이다. `_ref/` 는 배포에서 제외된다(`.gitignore`).

최종 갱신: 2026-08-18

---

## 1. 한 줄 정의

치지직·SOOP 스트리머를 위한 **무료 OBS 위젯·오버레이 모음**을 만들고 배포한다.
유료 서비스가 구독으로 파는 것을 무료로 풀어 사용자를 모으고,
크레딧 노출 → 커스텀 주문 → 프리미엄 순서로 수익화한다.

---

## 2. 전략 배경 (왜 이걸 하는가)

- 이 시장에서 사람들이 돈을 내는 건 기능이 아니라 **디자인 템플릿**이다.
  Streamlabs Ultra(월 ~$19), OWN3D Pro(월 ~$8–13), 테마 팩($5–30), Jamluca(위젯 구독)가 그 증거.
- 위젯 기술 자체는 이미 무료로 풀려 있음(StreamElements). 승부처는 **한국 감성 디자인 + 무료**.
- 국내 공백: 트윕·투네이션의 무료 기능 위젯 ↔ 크몽 유료 주문제작 사이,
  "무료 고퀄 템플릿" 자리가 비어 있다.
- 기술 구조 참고 모델: Overlayr — 카운트다운·골 바·티커를 전부 OBS 브라우저 소스 URL 하나로 제공.
  우리는 같은 구조를 무료 배포에 쓴다.

---

## 3. 하지 않는 것 (중요)

- **후원·결제 처리 기능** — 정산·수수료 사업이라 개인 불가. 트윕·투네이션 연동 가이드로 대체.
- **서버·백엔드·DB·로그인** — 서버비 0원 구조 유지. 정적 호스팅만 사용.
  (이 원칙 때문에 치지직·SOOP **채팅 자동 연동은 하지 않는다.** 대기열 위젯이 수동 관리 전용인 이유다.)
- **경쟁사 디자인·에셋·이름 복제** — 기능과 사업 모델 참고는 가능, 파일 복제는 IP 위반.
  재배포용 이미지는 자체 제작 / AI 생성 / 재배포 허용 라이선스만 사용.

---

## 4. 수익화 로드맵

1. 무료 배포 + 위젯 구석 크레딧 → 스트리머의 방송 화면이 곧 홍보
2. 커스텀 주문제작 유입 (크몽 연계)
3. 사용자가 쌓이면 프리미엄 기능 구독 전환 (Jamluca 모델)

---

## 5. 확정 사항 (원안의 "결정 대기" 항목 → 결론)

| 항목 | 결정 | 근거 / 구현 위치 |
|---|---|---|
| **브랜드 이름** | **온에어킷 (onairkit)** | 한글로 부르기 쉽고, 영문 표기가 그대로 저장소/도메인 이름이 된다. 5개 위젯의 `<title>`, 크레딧 문구, localStorage 키 접두사(`onairkit:`)에 모두 반영 완료 |
| **크레딧 노출** | **작게 상시 표시 + `credit=0` 으로 끄기 가능** | 위젯 오른쪽 아래 11px. 강제 노출은 무료 위젯의 채택률을 떨어뜨리므로 끌 수 있게 열어 둔다. 켜 두는 것을 README에서 부탁하는 방식 |
| **문의 창구** | **디스코드 단일 창구** | 버그 제보만 GitHub 이슈(기록·검색됨), 나머지 질문·요청·커스텀 문의는 전부 디스코드. 스트리머는 GitHub 계정이 없는 경우가 대부분이라 진입 장벽을 낮췄다. `.github/ISSUE_TEMPLATE/config.yml` 에서 빈 이슈를 막고 디스코드로 유도한다 |
| **크레딧 기본 문구** | **"온에어킷"** | `credit=1`(기본) 또는 값이 비면 "온에어킷". `credit=0` 은 숨김, 그 외 문자열은 그 문구로 교체(예: `credit=닉네임TV`) |
| **텔롭 변형 방식** | **`theme` 파라미터로 통합** (파일 분리 안 함) | `default` / `news` / `pastel` / `minimal` 4종. 파일을 나누면 유지보수가 4배가 되고 사용자도 어떤 파일을 받을지 헷갈린다. **theme은 텔롭 전용이 아니라 5종 공통 규약으로 승격했다** |
| **라이선스** | **MIT + 한국어 요약** | `LICENSE`. 상업 이용·수정·재배포 허용, 그대로 유료 판매는 "부탁" 수준으로만 명시(법적 제약 아님) |
| **GitHub 계정** | **조직 `onairkit`** (개인 계정 `dngus64bp-commits` 소유) | 개인 계정에 올리면 `dngus64bp-commits.github.io/onairkit/` 가 되는데, 크레딧을 보고 검색해 오는 유입 경로에 주소가 브랜드와 일치하는 편이 유리하다. 조직 생성은 무료 |
| **저장소 이름** | **`onairkit.github.io`** (점까지 포함) | 조직 대표 사이트 저장소라 주소에서 저장소 이름이 사라진다 → `https://onairkit.github.io/widgets/telop.html`. 이름을 `onairkit` 으로 지으면 경로가 한 번 더 붙는다 |
| **호스팅** | **GitHub Pages, `main` 브랜치 `/(root)`** | `.nojekyll` 포함. 절차는 `docs/배포가이드.md` |

---

## 6. 현재 상태

### 완료

| 산출물 | 상태 | 비고 |
|---|---|---|
| `widgets/telop.html` | **v1.1 완료** | 텔롭(자막). `slide`/`fade`/`ticker` 3모드, 문구 순환, hover 조작 UI(이전/일시정지/다음), 상태 저장. **`bar=0` = 바 배경 없이 글자만**(그림자 부착, 2026-08-20 추가) |
| `widgets/timer.html` | **v1.0 완료** | 타이머/카운트다운. `until`/`after`/`up`/`clock` 4모드, 진행 바, `onend` 3종, hover 조작 UI(±1분/일시정지/리셋) |
| `widgets/counter.html` | **v1.0 완료** | 카운터. 목표치(`goal`) 표시, `id` 슬롯으로 다중 사용, hover 조작 UI(−/+/리셋). **원안에서 v2로 미뤘던 방송 중 조작을 v1에 포함했다** |
| `widgets/queue.html` | **v1.0 완료** | 시참 대기열. 입력창으로 추가, 줄별 완료/삭제, `max` 초과분 "외 N명". 수동 관리 전용 |
| `widgets/roulette.html` | **v1.0 완료** | 룰렛. 2~12항목, `remove=1` 중복 없는 뽑기, 원판 클릭/Space로 회전 |
| 5종 공통 규약 | **확정·구현 완료** | `theme` `accent` `dark` `size` `pos` `margin` `credit` `reset` — 8개 파라미터가 5개 위젯에서 동일하게 동작 |
| `README.md` | **작성 완료** | 소개, 위젯 5종 표, 공통 파라미터, 30초 시작 가이드, 파일 구조, 라이선스, 기여 안내 |
| `docs/OBS설정법.md` | **작성 완료** | OBS 설치부터 문제 해결 8종까지. 초보자 기준 |
| `docs/배포가이드.md` | **작성 완료** | GitHub Pages 웹 UI 방식 + git 명령 방식 병기. gh CLI 없이 진행 가능 |
| `LICENSE` | **작성 완료** | MIT + 한국어 요약 3줄 |
| `index.html` | **작성 완료** | 랜딩 페이지 + URL 빌더. 위젯 5종 카드(실제 iframe 미리보기), 탭별 파라미터 폼, 라이브 미리보기, URL 복사, OBS 안내 3단계, FAQ 8개. 설정은 `onairkit:builder:v1` 키로 localStorage 저장 |

### 유료 팩 (비공개 폴더에서 진행)

유료 테마 팩은 `_운영/팩/` 아래에서 제작·관리한다(수익화 로드맵 3단계).
**진행 상태·가격·판매 전략은 공개 문서에 적지 않는다** — `_운영/팩/레트로아케이드/상태.md` 참고.
`_운영/` 은 `.gitignore` 로 배포에서 제외된다.

### 미완료 — 배포 전 반드시 필요

| 항목 | 상태 | 메모 |
|---|---|---|
| **git 첫 커밋** | **없음** | `git init` 만 되어 있고 커밋이 0개다. `widgets/`, `index.html`, `.gitignore`, `.nojekyll` 이 전부 untracked 상태 |
| ~~GitHub 주소 자리표시자~~ | **처리 완료** | 조직 `onairkit` + 저장소 `onairkit.github.io` 로 확정해 모든 파일의 주소를 실제 값으로 교체했다. 배포가이드도 조직 생성 절차로 다시 썼다 |
| ~~디스코드 초대 링크~~ | **처리 완료** | `https://discord.gg/vdMTFyNmZ4` (만료 없음 / 사용 횟수 무제한 / 임시 멤버 꺼짐). `discord/index.html` 에 반영했고 리다이렉트 동작까지 확인했다 |
| ~~`docs/` 운영 문서 공개 여부~~ | **처리 완료** | `배포전략.md` `수익화_실행계획.md` `홍보문구.md` 를 `_운영/` 으로 옮기고 `.gitignore` 에 추가했다. 단가·수익 계획이 공개 저장소에 올라가지 않는다. **앞으로 내부 문서는 `docs/` 가 아니라 `_운영/` 에 만들 것** |
| ~~디스코드 서버 생성~~ | **완료** | 서버 `OnAir_Kit` 생성·구성 완료. 카테고리 3 + 채널 7(텍스트 5 + 포럼 2), 커뮤니티 활성화, 인증 수준 중간, AutoMod 4종(한국어 욕설 포함, 경고는 `🔒-운영-로그`), 포럼 태그 16종(상태 태그는 운영자 전용), 본문 4개 게시·고정, `📢-공지`는 공지 채널(팔로우 가능). **초대 링크는 절대 삭제하지 말 것 — 복구 불가.** 습격 시에는 삭제 대신 `Server Settings → Invites → Pause Invites` |
| ~~서버 이름 표기~~ | **처리 완료** | `OnAir_Kit` → **`온에어킷`** 으로 변경. 서버명·크레딧·위젯 타이틀·랜딩·문서·GitHub 주소가 모두 한 브랜드로 일치한다 |
| ~~서버 아이콘~~ | **적용 완료** | ON AIR 램프 (민트 점 + 링 + `ON AIR`, 512×512). 원본과 후보 3종은 `_운영/아이콘/` 에 보관. 48px 축소 시에도 글씨가 읽히도록 램프·자간을 키운 보정판을 썼다 |
| **구글 폼 개설** | **미개설** | 문항 5개는 `_운영/배포전략.md` 4-2에 확정되어 있다. 만들어서 랜딩·README에 연결 |

---

## 7. 다음 작업 (우선순위 순)

### 1순위 — 배포 가능 상태 만들기

1. ~~**랜딩 페이지 `index.html` 작성**~~ → **완료.** 위젯 5종 카드(실 iframe 미리보기) + 탭별 URL 빌더 +
   라이브 미리보기 + URL 복사 + OBS 안내 + FAQ까지 들어갔다. 위젯과 같은 원칙(파일 하나, vanilla, 빌드 없음)을 지켰다.
2. **커스텀 제작 문의 창구 확정** → `index.html` 푸터의 `TODO` 배지 4개와 README의 `(준비 중)` 을 실제 링크로 교체.
   **랜딩에 노란 TODO 배지가 그대로 보이는 상태로는 공개하면 안 된다.**
3. **GitHub Pages 배포 실행** (`docs/배포가이드.md` 순서대로)
   - 첫 커밋 → push → Settings > Pages > `main` `/(root)` → 주소 확인
   - 배포 체크리스트 8항목 전부 통과 확인
4. **README의 위젯 링크를 실제 Pages 주소로 교체**
   - 지금은 저장소 상대 경로다. 배포 후에는 "바로 열어 보기" 링크가 실제로 위젯을 띄워야 한다

### 2순위 — 커뮤니티 검증

5. **실사용 테스트** — 본인 PC에서 OBS에 5종을 전부 올려 30분 이상 방송 환경으로 돌려 본다.
   특히 장면 전환, OBS 재시작, 상호작용 창 조작을 반복해 상태 저장이 실제로 버티는지 확인
6. **커뮤니티 공개** — 스트리머 커뮤니티(디시 스트리머 갤러리, 관련 디스코드, 트위터/X, 커뮤니티 게시판)에 무료 공개
   - 소구점은 "무료 + 설치 없음 + 한국어 설명" 세 가지로 고정
   - 글에는 반드시 **URL 빌더 랜딩 페이지 링크**를 건다. 저장소 링크만 걸면 이탈한다
7. **피드백 수집 채널 운영** — Issues 템플릿 정리, 자주 나오는 질문은 `docs/OBS설정법.md` 문제 해결에 계속 추가

### 3순위 — 피드백 반영 및 확장

8. **요청 기반 위젯 추가** — 무엇을 만들지 미리 정하지 않는다. 실제 요청이 2건 이상 겹치는 것부터 만든다.
   후보(추정): 알림 배너, 목표 진행 바(구독/팔로우 골), 최근 후원자 티커, 채팅 오버레이(수동), 방송 종료 카운트다운
9. **테마 추가** — `theme` 값 확장이 파일 추가보다 항상 우선이다. 5종 공통으로 동시에 적용되어야 한다
10. **문서 보강** — 실제로 들어온 질문만 반영한다. 안 물어본 것을 미리 쓰지 않는다

### 4순위 — 수익화 전환 판단

11. **커스텀 주문제작 유입 시작** — 랜딩 페이지에 "내 채널 전용 제작" 섹션 노출, 크몽 판매글 연결
12. **프리미엄 전환 시점 판단 기준** (아래 조건을 만족하기 전에는 유료화하지 않는다)
    - 조건 A: 실사용자 추정 **100명 이상** (Issues·문의·커뮤니티 반응으로 추정)
    - 조건 B: **같은 유료 기능 요청이 반복적으로** 들어옴 (내가 상상한 기능이 아니라 사용자가 말한 것)
    - 조건 C: 무료 위젯이 **최소 8종 이상**으로 충분히 쌓여 무료판만으로도 만족스러움
    - 위 3개를 모두 만족한 뒤에야 유료 라인을 만든다. **기존 무료 위젯을 유료로 돌리는 일은 절대 하지 않는다.**
      프리미엄은 항상 "새로 추가되는 것"이어야 한다
13. **유료화 형태 후보** — 프리미엄 테마 팩(일회성 구매) > 구독. 서버가 없으므로 구독 관리는 부담이 크다

---

## 8. 기술 원칙

- **위젯 1개 = HTML 파일 1개.** vanilla HTML/CSS/JS. 프레임워크·빌드 도구 없음.
- 설정 이중화: 파일 상단 `CONFIG` 객체(초보자용, 메모장 수정) + **URL 파라미터**(CONFIG를 덮어씀).
- 배경 투명, `overflow:hidden`. OBS 브라우저 소스 **1920×1080** 기준으로 디자인.
- 폰트: Pretendard(jsdelivr CDN) + 시스템 폴백. CDN이 죽어도 위젯은 정상 동작해야 한다.
  추가 한글 폰트는 상업용 무료 + 재배포 가능 라이선스 확인 후 사용.
- `prefers-reduced-motion` 대응, 창 리사이즈 대응, 파라미터 값 검증(clamp).
- 상태 저장은 `localStorage`, 키 접두사 `onairkit:`. **저장소가 막힌 환경에서도 위젯은 동작해야 한다**(저장만 안 됨).
- 호스팅: GitHub Pages.

---

## 9. 새 위젯 공통 규약 (반드시 지킬 것)

기존 5종이 이미 이 규약을 따르고 있다. 새 위젯을 추가할 때 하나라도 어기면 사용자가 위젯마다 다시 배워야 한다.

### 9-1. 파일

- 파일 하나로 동작하고, **더블클릭 미리보기**가 가능할 것.
- **기본값만으로 바로 그럴듯하게** 보일 것(데모 문구/데모 데이터 포함).
- 파일 상단에 한국어 주석 블록: OBS 설정법 + 파라미터 표 + 예시 URL + 조작법.
  **이 주석이 그 위젯의 정본 문서다.** README와 어긋나면 주석이 맞다.
- `<title>` 은 `온에어킷 · 〈위젯 이름〉` 형태.

### 9-2. 공통 파라미터 (8개, 전 위젯 동일)

| 이름 | 의미 | 기본값 | 허용 범위 |
|---|---|---|---|
| `theme` | 색·모서리·그림자 프리셋 | `default` | `default` / `news` / `pastel` / `minimal` |
| `accent` | 포인트 색 hex (`#` 생략 가능) | theme 기본값 | 3자리/6자리 hex. 아니면 무시 |
| `dark` | 1=다크 / 0=라이트 | theme 기본값 | `1` / `0` |
| `size` | 기준 글자 크기(px) | 위젯별 | 16 ~ 120 |
| `pos` | 화면 안 위치 | 위젯별 | `tl` `tc` `tr` `bl` `bc` `br` `c` |
| `margin` | 화면 가장자리 여백(px) | `48` | 0 ~ 400 |
| `credit` | 크레딧 | `1` | `1` 표시 / `0` 숨김 / 그 외 문자열은 문구 교체 |
| `reset` | 저장 상태 무시하고 초기화 | (없음) | `1` |

### 9-3. 파라미터 처리 규약

- **URL 파라미터가 `CONFIG` 를 덮어쓴다.** 항상 이 방향이다.
- 값이 **비어 있으면(`?size=`) 없는 것으로 보고 기본값**을 쓴다.
  단 "비우면 숨김"이 의미가 되는 문자열 파라미터(`label`, `title`, `icon`)만 예외로 빈 값을 "숨김"으로 해석한다.
  이 예외는 반드시 파일 상단 주석에 명시할 것.
- 숫자는 **clamp**, 목록은 **화이트리스트**, 색은 **hex 정규식 검증**. 통과 못 하면 조용히 기본값.
  **어떤 입력에도 위젯이 깨지거나 빈 화면이 되어서는 안 된다.**
- 여러 항목을 받는 파라미터는 **`|`(세로줄) 구분**으로 통일한다(`text`, `names`, `items`).
  세로줄을 글자로 넣을 수 없다는 제약을 주석에 반드시 적는다.
- 모르는 파라미터는 **조용히 무시**한다. (사용자가 캐시 무력화용 `?v=2` 를 붙여도 안전해야 한다)

### 9-4. theme 규약

| 값 | 성격 | 기본 accent | 기본 dark | radius |
|---|---|---|---|---|
| `default` | 네온 그린, 게임 방송 기본 | `#00ffa3` | 1 (다크) | 14px |
| `news` | 속보 레드, 각진 느낌 | `#ff2d55` | 1 (다크) | 4px |
| `pastel` | 파스텔 핑크, 부드러움 | `#ff8fb1` | 0 (라이트) | 22px |
| `minimal` | 흑백 미니멀, 그림자 최소 | `#1a1a1a` | 0 (라이트) | 8px |

- **theme은 색·모서리·그림자만 바꾼다. 레이아웃은 절대 바꾸지 않는다.**
- theme이 `accent` 와 `dark` 의 **기본값을 정하고**, 사용자가 `accent`/`dark` 를 직접 주면 그쪽이 우선이다.
- 새 theme을 추가하면 **5종 전부에 동시에** 넣는다. 한 위젯에만 있는 theme은 만들지 않는다.

### 9-5. 크레딧 규약

- 위치: 위젯 카드 **오른쪽 아래 바깥**, `font-size:11px`, `font-weight:600`, 불투명도 34%(라이트 30%).
- `pointer-events:none`, `user-select:none`.
- **위젯이 화면 위쪽(`tl`/`tc`/`tr`)에 있으면 크레딧을 카드 아래로 뒤집어** 화면 밖 잘림을 방지한다.
- 기본 문구는 **"온에어킷"**. `credit=0` 이면 `display:none`, 그 외 문자열이면 그 문구로 교체.
- 5종이 동일한 CSS 블록을 쓴다(각 파일에 `/* 크레딧 (온에어킷 공통 코드) */` 주석으로 표시).

### 9-6. hover 조작 UI 규약

OBS 송출 화면에는 마우스 커서가 없다. 조작은 **소스 우클릭 → "상호작용" 창**에서만 가능하다.
이 제약이 조작 UI 설계의 전부다.

- 평소: `opacity:0` + `pointer-events:none` → **방송 화면에 절대 나가지 않는다.**
- hover 시: `opacity:1` + `pointer-events:auto` → 상호작용 창에서만 보인다.
- 버튼은 위젯이 위쪽 배치면 **아래에**, 아래쪽 배치면 **위에** 나타나 화면 밖 잘림을 막는다.
- **키보드 단축키를 반드시 함께 제공**한다. 상호작용 창에서 마우스보다 빠르다.
  공통 관례: `Space` = 주 동작(토글/실행), `R` = 리셋, 방향키 = 값 증감/이동.
  `R` 은 한글 자판(`ㄱ`)에서도 동작하게 처리한다.
- 조작할 것이 없으면 버튼을 아예 렌더링하지 않는다(텔롭 문구 1개, ticker 모드 등).
- 진행 중인 애니메이션(룰렛 회전 등) 동안에는 입력을 전부 무시한다.

### 9-7. 상태 저장 규약

- `localStorage`, 키는 `onairkit:<위젯>:<항목>` 형태. 예: `onairkit:counter:value`, `onairkit:queue:list`.
- 같은 위젯을 2개 이상 쓸 수 있는 위젯은 **`id` 파라미터로 슬롯을 분리**한다(카운터가 선례).
  `id` 생략 시 키는 기존 형태 그대로 유지해 하위 호환을 깨지 않는다.
- `reset=1` 이면 저장값을 무시하고 CONFIG/URL 값으로 초기화한다.
- **저장소 접근이 막힌 환경(시크릿 모드, 정책 차단)에서도 위젯은 정상 동작해야 한다.** 저장만 생략된다.

---

## 10. 디자인 가이드

- 감성: 한국 예능·뉴스 자막. 치지직·SOOP 게임 방송 화면 위에서 잘 보일 것(drop-shadow, 반투명).
- 기본 다크 + 라이트 옵션(`dark=0`) 필수.
- 모션은 cubic-bezier로 부드럽게, 과하지 않게. 등장 애니메이션은 1회.
- 긴 텍스트 잘림 방지: max-width + ellipsis.
- 이모지는 절제한다. `counter` 의 `icon` 처럼 **사용자가 직접 넣는 자리**에만 허용한다.

---

## 11. 테스트 방법

1. **브라우저에서 직접 열기** — 가장 빠른 확인. 위젯 파일을 더블클릭하고 주소창에서 파라미터를 바꿔 본다.
   ```
   file:///C:/onairkit/widgets/telop.html?text=테스트|둘째 문구&mode=ticker
   ```
2. **OBS 로컬 파일 방식** — 소스 `[+]` → 브라우저 → "로컬 파일" 체크 → 파일 선택 → 1920×1080.
3. **OBS 파라미터 방식** — "로컬 파일" **해제** 후 URL 칸에
   `file:///경로/telop.html?text=...` 형식으로 입력. (초보자가 가장 많이 막히는 지점)
4. **상호작용 창** — 소스 우클릭 → "상호작용" → 위젯 위 hover로 버튼 확인 → 단축키 확인.
5. **회귀 확인 체크리스트** (위젯을 고칠 때마다)
   - [ ] 파라미터 없이 열었을 때 데모가 그럴듯하게 나온다
   - [ ] `theme` 4종이 전부 정상이고 레이아웃이 동일하다
   - [ ] `pos` 7종에서 화면 밖으로 잘리지 않는다 (크레딧 포함)
   - [ ] 이상한 값(`size=-999`, `theme=zzz`, `accent=hello`)을 넣어도 안 깨진다
   - [ ] OBS 재시작 후 저장 상태가 유지되고, `reset=1` 로 초기화된다
   - [ ] 상호작용 창 밖(송출 미리보기)에서는 조작 버튼이 보이지 않는다
   - [ ] 인터넷을 끊어도(폰트 CDN 차단) 위젯이 정상 동작한다

---

## 12. 파일 구조

```
onairkit/
├── CLAUDE.md              이 문서 (프로젝트 기준 문서)
├── README.md              사용자용 소개
├── LICENSE                MIT + 한국어 요약
├── .nojekyll              GitHub Pages가 파일을 그대로 서빙 (삭제 금지)
├── .gitignore             _ref/ · _운영/ 및 OS·편집기 파일 제외
├── index.html             랜딩 페이지 + URL 빌더 (작성 완료)
├── THIRD-PARTY-NOTICES.md 서드파티 고지 (Pretendard / SIL OFL 1.1)
│
├── discord/index.html     디스코드 초대 리다이렉트
│                          ★ 초대 링크는 이 파일의 INVITE 한 줄에서만 관리한다.
│                            커스텀 URL(discord.gg/onairkit)은 부스트 14개 필요 → 무료 불가
│
├── .github/ISSUE_TEMPLATE/  GitHub 이슈 템플릿 3종 + 디스코드 유도 설정
│   ├── bug-report.yml       버그 신고 (사용한 URL 전체가 필수 항목)
│   ├── feature-request.yml  위젯·기능 요청
│   ├── show-and-tell.yml    사용 자랑 — 사실상 유일한 사용자 수 측정 수단
│   └── config.yml           빈 이슈 차단 + 디스코드/문서/빌더 링크
│
├── widgets/               위젯 본체. 파일 하나 = 위젯 하나
│   ├── telop.html
│   ├── timer.html
│   ├── counter.html
│   ├── queue.html
│   └── roulette.html
│
├── docs/                  공개 — 사용자용 문서만 둔다
│   ├── OBS설정법.md
│   └── 배포가이드.md
│
├── _운영/                  비공개 (.gitignore) — 단가·수익·홍보 초안
│   ├── 배포전략.md
│   ├── 수익화_실행계획.md
│   └── 홍보문구.md
│
└── _ref/                  작업용 참고 자료 (.gitignore로 배포 제외)
    ├── 원안_계획서.md      최초 계획서. 이 문서의 원본
    └── telop_widget_v0.1.html
```
